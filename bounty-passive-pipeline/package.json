{
  "name": "bounty-passive-pipeline",
  "version": "0.1.0",
  "description": "Read-only browser-only bug bounty metadata collector",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "npm run build && playwright test",
    "pipeline": "npm run build && node dist/index.js",
    "discover": "npm run build && node dist/index.js --discover-only",
    "osint": "npm run build && node dist/src/osint/index.js"
  },
  "author": "",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.2",
    "@types/better-sqlite3": "^7.6.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  }
}
