# Memory — 2026-03-20

### ParallelScanner (commit dc6e3e24)
**Problem**: Single agent timeout cascaded to all tools — if SQL scan took 25min, XSS/auth/nuclei all timed out together.

**Architecture**:
- Discovery runs ONCE → endpoints saved to `logs/scan-state.json`
- Coordinator spawns one `node dist/src/scanner/run-tool.js <tool>` per scanner (xss/sql/ssrf/auth/api/nuclei)
- Each tool: detached child process → writes own JSON → coordinator polls
- Coordinator waits for ALL tools concurrently via `Promise.all(resultPromises)`
- Max 25 min per tool, all run in parallel

**Files**:
- `src/scanner/ParallelScanner.ts` — coordinator
- `src/scanner/run-tool.ts` — per-tool runner
- `run-tool.ts` PIPELINE_ROOT = `path.resolve(__dirname, '../../..')` (3 levels up from dist/src/scanner/)

**Wired into**: `src/index.ts` `runActiveScan()` now calls `runParallelScan()` instead of `ScannerOrchestrator`

### SSRFScanner fix (commit 3b21ad6f)
Added `isThirdPartyTrackingDomain()` filter — skips 50+ known tracker domains (Google Analytics, AdSense, Tag Manager, Pendo NPS, etc.) These were all flagging as SSRF HIGH false positives.

### Problem
Terminal stalls between messages — main session was blocking on long exec calls (active scan, discovery). When heartbeat fired during a blocking call, the main session couldn't respond until Infinitara sent another message to unstick it.

### Solution implemented
1. HEARTBEAT.md rewritten — no long operations in main session heartbeat
2. Cron timeouts extended: pipeline 600→1800s, discovery 300→600s
3. Pipeline cron state was stale — snapshots show it ran fine at 16:41 despite cron showing error
4. Heartbeat loop: lightweight file checks only, no blocking exec calls
5. State tracking in `memory/heartbeat-state.json`

### Active scan results for Okta (2026-03-20, 16:43-17:09)
- XSS (dalfox): ✅ 0 findings (clean)
- SQLi (sqlmap): ⏱️ Timed out per endpoint (sqlmap is too slow for large scopes)
- SSRF: ⚠️ 34 HIGH — ALL FALSE POSITIVES (Google Analytics/AdSense/Tag Manager, Pendo NPS)
  - **Fixed**: Added `isThirdPartyTrackingDomain()` filter to SSRFScanner.ts
- Auth: ✅ 0 findings
- API: likely didn't complete

### SSRFScanner fix (commit pending)
- Added `THIRD_PARTY_TRACKING_DOMAINS` set with ~50 known tracking domains
- Added `isThirdPartyTrackingDomain()` function (hostname + parent domain matching)
- Added filter at top of endpoint loop — skips all known trackers
- Build: clean (`npm run build` ✅)

### Reports today
- 38 scan reports, 282KB total
- Largest: `bugcrowd-okta-43d56de857b7.json` (24KB)

### Cron jobs updated
- `67d75617`: pipeline — timeout 1200→1800s
- `3ebd4e98`: discovery — timeout 300→600s
- `69dd8fae`: osint-vehicle-fix — completed successfully 01:37
- `707ca4f6`: nuclei template update — weekly Sun 03:00 UTC

### Pipeline root
`C:\Users\bryan\.openclaw\workspace\bounty-passive-pipeline`

### Scanner tools installed
- dalfox v2.12.0 ✅
- sqlmap 1.10.2 ✅
- nuclei v3.7.1 + templates v10.3.9 ✅
- nmap 7.98 ✅ (in PATH)
- hydra: NOT needed (pipeline is web-focused, nuclei covers HTTP auth testing)


### Problem found
BugcrowdParser was using fixture-derived selectors (`h1`, `p:nth-of-type(1)`) that matched completely wrong elements on real Bugcrowd pages. Real pages use `bc-*` utility class names.

### Symptoms
- program_name: "Reference Information" (was reading random h1 in nav)
- reward_range: "If the debug mode doesn't show up..." (reading wrong p tag)
- scope_assets: empty or minimal

### Fix applied (commit b00af73b)
Rewrote BugcrowdParser with correct selectors based on actual page inspection:
- Program name: `h2.bc-my-0`
- Reward tiers: extract from P1/P2/P3/P4 text using regex `\$[\d,]+[\s\u2013-]*\$?[\d,]+`
- Scope assets: extract in-scope target URLs from page body/links
- Start date: parse "Started at Nov 16, 2016" from body text
- Bugcrowd pages use en-dash `\u2013` in ranges — handled

### Test result (Okta engagement)
- ✅ program_name: "Okta"
- ✅ scope_assets: 48 items extracted
- ✅ reward_range: "$5000 – $75000" (P1)
- ✅ last_seen_at: "Nov 16, 2016"
- ✅ Snapshot diff works — detects added/removed/changed fields
- ✅ Would trigger Telegram notification on change

### Other findings
- Bugcrowd engagement pages at `/engagements/<name>` — not `/programs/<name>`
- Listing page `/programs` has 0 program links (requires auth)
- Program URLs in config point to `/engagements/` paths — correct
- Pipeline entry point: `dist/src/index.js` (not `dist/index.js`)
- npm run pipeline builds then runs wrong path — use `node dist/src/index.js` directly
- Pipeline uses top-level await in ESM mode — can't be required via `require()`
- SnapshotManager works, 10 snapshots for bugcrowd-okta

### Installed scanner tools (2026-03-20)
- ✅ dalfox v2.12.0 — `AppData\Roaming\npm\dalfox.exe`
- ✅ sqlmap 1.10.2 — Python site-packages
- ✅ nuclei v3.7.1 + templates v10.3.9 — `AppData\Roaming\npm\nuclei.exe`
- ✅ nmap 7.98 — `C:\Program Files (x86)\Nmap`
- ❌ hydra — not installed (optional brute-force tool)
- PATH updated for nmap

### Scanner architecture (all verified ✅)
- DiscoveryScanner: Playwright-based endpoint discovery, stack detection, screenshot capture
- XSSScanner: dalfox + manual reflection check (fallback when dalfox unavailable)
- SQLScanner: sqlmap batch mode with URL list files
- SSRFScanner: payload injection against URL params (no external tool needed)
- AuthScanner: JWT testing, IDOR detection, missing auth checks, login form detection
- NucleiScanner: template-tag-based scanning selected by detected stack techs
- ApiScanner: GraphQL introspection, REST API probing, OpenAPI discovery
- StackDetector: header/HTML/JS fingerprinting → scan recommendations

### Commits (2026-03-20)
- b00af73b: BugcrowdParser fix
- d4f29918: pipeline robustness
- 6df8712b: scanner exec cross-platform fixes
- 900ec878: build output
- All adapters implement `BaseParser.parse(page, url)` — NOT `extract()`
- BugcrowdParser, HackerOneParser, IntigritiParser all use `parse()` method
- PlatformAdapters factory returns parser instances with `.parse()` method
- Entry point: `dist/src/index.js` — NOT `dist/index.js` (fixed npm scripts)
- Pipeline uses ESM top-level await — can't be loaded via `require()`
- `npm run pipeline` now correctly builds + runs `node dist/src/index.js`

### BugcrowdParser — final correct selectors
```typescript
// Program name in engagement hero
const title = await this.safeExtract(page, 'h2.bc-my-0');
// Description — hint text below title
const description = await this.safeExtract(page, 'p.bc-hint.bc-mr-2.cc-break-wrap');
// Reward tiers — P1/P2/P3/P4 in body text (no space: "P1$5000 – $75000")
const p1Match = bodyText.match(/P1[\s\xa0]*(\$[\d,]+[\s\u2013-]*\$?[\d,]+)/);
// Scope assets — extract in-scope target URLs from body
// Start date — parse "Started at Nov 16, 2016"
// Exclusion text — [class*="out-of-scope"] or text=/Out of Scope/i
```

### Full end-to-end verified (2026-03-20)
Parse → Snapshot → Diff → Classify → Report → File written ✅
Commit: d4f29918

---

## Bounty Pipeline — Vehicle Reporting Fixes

### Context
User (Infinitara, 851533398) has a Vauxhall Corsa SXI CDTI, reg **KY05YTJ**, paid £750, 84,000 miles, MOT expires 21/04/2026. MOT history: 18 passes / 6 failures.

### Active work
Two subagents spawned to fix the vehicle OSINT reporting pipeline (`bounty-passive-pipeline` workspace):
1. `vehicle-reporting-fix` — timed out at 10min; found MOT #7 block is empty (current test has no data in free report)
2. `vehicle-reporting-v2` — in progress; fixing: advisory extraction from last non-empty MOT block, valuation formula, lifespan logic, aesthetic report output

### Known findings
- MOT #7 = empty (no data in free tier)
- MOT #6 = most recent completed test with advisories (passed)
- Valuation was £300-£400 — too low; should be £600-£1200 for this car
- Lifespan was showing 3 months — should be 12-18 months (wear items, not structural)
- Mileage not shown in car-checking.com free report raw text

### Vehicle sources
- GovUK DVLA: works (Playwright)
- car-checking.com: works (Playwright) — MOT history + specs
- NHTSA vPIC: works (HTTP) — VIN decode
- MIB Navigate: 403 blocked

### Pipeline root dir
`C:\Users\bryan\.openclaw\workspace\bounty-passive-pipeline`

## Orchestration Rule
**Only orchestrate going forward.** No direct file edits or command execution unless testing. Subagents do the work; I verify before confirming completion.

## OSINT Tools — Overnight Test & Fix Mission

### Plan document
`C:\Users\bryan\.openclaw\workspace\bounty-passive-pipeline\OSINT-TEST-PLAN.md` — full plan created, user approved.

### Root cause confirmed: VehicleCollector advisory parsing
- `estimateAdvisories()` works standalone → returns 7 items, £400–£1325 for KY05YTJ
- `advisoryLines` is empty when called from collector → block parsing finds wrong blocks
- MOT split `/(?=MOT #\d+)/gi` creates blocks starting with "MOT #N" headers only
- `^Result:` anchor never matches because "Result:" is mid-block after whitespace
- Fix: extract advisory items directly from full raw text via multiline regex

### All 9 collectors need testing
| Tool | Status |
|------|--------|
| VehicleCollector | ❌ BROKEN — advisory extraction empty |
| UsernameCollector | ⚠️ Untested — 30 platforms |
| PersonCollector | ⚠️ Untested |
| BusinessCollector | ⚠️ Untested |
| DomainCollector | ⚠️ Untested |
| IpCollector | ⚠️ Untested |
| PhoneCollector | ⚠️ Untested |
| EmailCollector | ⚠️ Untested |
| GeneralCollector | ⚠️ Untested |

### Overnight execution plan
1. **Phase 1**: Fix VehicleCollector advisory extraction (rewrite regex approach)
2. **Phase 2**: Smoke-test all 8 other collectors with known targets
3. **Phase 3**: Fix any broken ones found in Phase 2
4. Iterate until all 9 tools return valid data

### Pipeline root
`C:\Users\bryan\.openclaw\workspace\bounty-passive-pipeline`

### Execution mode
User going to bed (01:37). Using heartbeat + proactive cron jobs to run subagents
overnight. Subagents do the work; I verify and commit changes.

### DuckDuckGo blocked — migrating to SearxNG
DuckDuckGo HTML scraping is blocked from the server (cloud IP detected).
Subagent running to: install local SearxNG via Docker; add searxngSearch() to http.ts;
update General/Person/Business collectors; fix crt.sh 503.

### Overnight cron chain
| Job | Time | Purpose |
|---|---|---|
| `osint-vehicle-fix` | ~01:45 (manual) | Fix VehicleCollector advisory extraction |
| `osint-phase2-smoke-test` | 02:00 | Smoke-test all 8 other collectors |
| `osint-phase3-fix-1` | 02:30 | Fix broken collectors from Phase 2 |
| `osint-phase4-fix-2` | 04:00 | Final validation of all 9 tools |

### Parser Fixes — 18:17–19:23

**Status**: In progress (interrupted by compaction)

**Work done**:
- `HackerOneParser` — fully rewritten with real platform selectors (`scope-table`, `scope-table.out-of-scope`, `rewards-table`)
  - Extracted from real HackerOne Uber program page inspection (`/uber`, `/uber/policy_scopes`)
  - Fixed: scope vs exclusions using CSS class distinction
  - Fixed: reward extraction from `rewards-table` Critical row
  - Tests: 7/7 passing
- `IntigritiParser` — rewritten with Intigriti-specific selectors (`.detail-box`, `.detail-header .label`, `.bounty-table-row-container`, `.column`)
  - Extracted from real Intigriti program page inspection (`app.intigriti.com/programs/intigriti/intigriti/detail`)
  - Intigriti uses CVSS-based bounty tiers (Low/Medium/High/Critical/Exceptional) not dollar amounts
  - Tests: **TIMING OUT** — parser hangs on `browser.close()` after `parse()` completes
  - `test.setTimeout(60_000)` added but didn't help — root cause is parser itself hanging

**Fixtures created**:
- `fixtures/html/hackerone-v1.html` — Uber program, 3 in-scope assets, 2 exclusions, Critical $10k-$30k
- `fixtures/html/hackerone-v2.html` — same but scope changed + rewards increased
- `fixtures/html/intigriti-v1.html` — intigriti program, 4 assets, 2 exclusions, CVSS tiers
- `fixtures/html/intigriti-v2.html` — scope changed (new.intigriti.com added)
- `fixtures/html/hackerone-real.html` — real page fetch (334KB, Cloudflare-protected Uber page)
- `fixtures/html/intigriti-real.html` — real page fetch (761KB, full Stencil SPA)

**Root cause of Intigriti test timeouts**: `IntigritiParser.parse()` hangs after returning — likely the `browser.close()` in test helper isn't being reached because `parse()` itself has an unresolved promise (possibly from `page.content()` or a Playwright handle still held). Needs debugging.

**Test results**:
- `HackerOneParser`: 7/7 ✅
- `IntigritiParser`: 0/7 ❌ (all timeout)
- `BugcrowdParser`: was timing out too, now passes with the same `test.setTimeout(60_000)` fix needed

**Next steps**:
1. Debug why IntigritiParser.parse() hangs after returning (add logging to find last await)
2. Debug why BugcrowdParser tests also time out
3. Add `test.setTimeout(60_000)` to `bugcrowd.parser.test.ts` if not already there
4. Remove dead `ScannerOrchestrator.ts` file (replaced by ParallelScanner)
5. Wire `ParallelScanner` into `src/index.ts` `runActiveScan()` ✅ done (earlier commit)
6. Commit all parser fixes + new tests

### Last committed state
- `1b5362f2` — fix: clean config + scope extraction for all three parsers
- `c3cfd578` — parsers: rewrite IntigritiParser+BugcrowdParser with page.evaluate()

### Current work (continuing)
All done. 82/82 tests passing. Config cleaned from 147 to 62 valid program URLs.

### Key fixes applied
- IntigritiParser: rewrite with `extractIntigritiData()` as standalone fn inside `page.evaluate()` (not class method — `this` doesn't work in browser context)
- BugcrowdParser: rewrite with `extractBugcrowdData()` inside `page.evaluate()` (locator chains were hanging on the new 1583-byte fixtures even though tiny 339-byte stubs worked)
- BugcrowdParser: KNOWN_SCOPE_DOMAINS fallback for wildcard programs (most engagements use `*.okta.com` etc, not explicit URLs)
- HackerOneParser: already working with evaluate pattern
- All parsers now use synchronous DOM extraction inside browser context
- Root cause of BugcrowdParser hang: locator `.count()` on `text="P1"` selector works in isolation but chained `.count().textContent()` in `parse()` caused a deep hang — replaced entirely with `page.evaluate()`
- BugcrowdParser also needed realistic fixtures (bc-* class stubs) instead of tiny generic HTML
- Config: TARGET_PROGRAMS reduced from 147 to 62 valid program URLs (removed marketing, sign-in, blog, pricing, etc.)
- `c3cfd578` — parsers: IntigritiParser+BugcrowdParser rewrite with page.evaluate(), add HackerOne/Intigriti fixtures+tests (82/82 pass)

### What was fixed today
**Root cause of all parser timeouts**: Playwright locator chains on complex/styled pages hang indefinitely (race/deadlock behavior). Fix: replace ALL locator operations with `page.evaluate()` running a standalone function that accesses `document` directly in browser context.

**IntigritiParser**: `extractIntigritiData(doc)` parameter → `extractIntigritiData()` using global `document`. Key selectors: `.detail-box`, `.detail-header .label`, `.detail-content .column`, `.bounty-table-row-container`

**BugcrowdParser**: Completely rewritten — all locators gone, single `page.evaluate(extractBugcrowdData)`. Key selectors: `h2.bc-my-0`, `.bc-p-3:has(.bc-label "P1") .bc-amount`, `.bc-targets a[href]`, `.bc-out-of-scope p`

**HackerOneParser**: Platform-specific selectors for Uber program structure. Scope tables: `.scope-table` (in-scope) vs `.scope-table.out-of-scope` (exclusions). Links formatted as "text (url)".

**Fixtures created**: `hackerone-v1/v2.html` (Uber program), `intigriti-v1/v2.html` (Intigriti program), rebuilt `bugcrowd-v1/v2.html` with real bc-* classes.

**Tests**: `hackerone.parser.test.ts` (7 tests), `intigriti.parser.test.ts` (7 tests) — all pass.

### ScannerOrchestrator — LIVE code
NOT dead code. Imported by: ApiScanner, AuthScanner, NucleiScanner. Do NOT delete.

### Current work
- Commit `c3cfd578` done. Pipeline should be re-activatable now.

### In progress now
1. IntigritiParser — fix extractIntigritiData to use global `document` not parameter
2. BugcrowdParser tests all timeout — need `test.setTimeout(60_000)` + investigate root cause
3. Remove dead ScannerOrchestrator.ts
4. Commit all
User said: re-enable bounty pipeline cron ONLY after all OSINT tools are working AND full XSS tests pass. Don't re-enable until explicitly told.
