# 2026-03-22 — Session Notes

## Pipeline Analysis Complete — All Issues Documented

Full end-to-end analysis done. Here's the complete bug inventory:

### 🔴 CRITICAL (pipeline broken)
1. **Discovery platforms all broken**:
   - Bugcrowd: returns 406 on API calls
   - Intigriti: SPA, JS-rendered scope (Playwright needed but untested)
   - Standoff365: ALL programs private/invite-only
   - HackerOne: scope_assets returns empty array
   - → Pipeline has no valid programs to scan

2. **ScannerOrchestrator parallel crash**: Uses `Promise.all(promises)` then awaits them — this still fires all tools concurrently, defeating the sequential fix from commit `2415706f`. Fix: change to `for...await` loop over `scanPromises` (same pattern already used in step scripts).

3. **GauScanner null-domain crash**: `baseDomains.add(null)` when `extractBaseDomain` returns null. Set silently ignores the null but also ignores the return value of `add()`. Will cause null entries in the set. Fix: filter null before adding.

### 🟠 HIGH (tools non-functional)
4. **nuclei exit 1 on every run**: Template paths were wrong (`vulnerabilities/` instead of `http/vulnerabilities/`). Fixed, rebuild done. Still needs verification.

5. **httpx Python/Go conflict**: `httpx` on PATH resolves to Python script (wrong). Go binary is at `C:\Users\bryan\go\bin\httpx.exe`. All exec calls must use full path or shim.

6. **dalfox hangs indefinitely**: No per-call timeout. AbortSignal.timeout() was added to XSSScanner but dalfox itself hangs — needs sigkill fallback or pkill.

### 🟡 MEDIUM (results wrong/broken)
7. **SubfinderScanner type mismatch**: `targets.filter` receives `ScanTarget[]` but code assumed `string[]`. Already has `.map` fix in place (since commit `99e5e1de`). ✓ FIXED

8. **GauScanner same type issue**: Already has `.map` normalization. ✓ FIXED

9. **nuclei JSONL parsing untested**: `parseNucleiOutput` reads from `-json-export` file but nuclei exit code 1 = error. Needs stderr captured. Also: nuclei banners go to stderr on exit 0, making stderr capture unreliable.

10. **SQLScanner sqlmap hangs**: Same as dalfox — per-call timeout added but sigkill fallback missing.

### 🔧 Verified Working
- subfinder: 11k+ okta.com subs found, 443 prod-quality
- httpx (Go): 30-36 alive okta.com subdomains confirmed
- nuclei templates: `C:\Users\bryan\.nuclei-templates`, correct dirs: `http/vulnerabilities/`, `http/exposed-panels/`, `http/exposures/`, `http/misconfiguration/`
- All step scripts use sequential for...await (commit `2415706f`)
- Build clean: `npm run build` succeeds

## What Was In Progress
- NucleiScanner.ts fully rewritten, built clean
- Systematic fix-and-test plan defined but not yet executed
- Cron jobs remain DISABLED (pipeline paused)

## Files to Clean Up (temp scripts)
`\_fullscan.cjs`, `\_osint.cjs`, `\_ntest.cjs`, `\_rewrite-nuclei.cjs`, `\_write-nuclei.cjs`, `\_build.ps1`, `\_fix-ts.ps1`, `\_fix2.ps1`, `\_probe.ps1`, `\_nuclei.ps1`, `\_nuclei2.ps1`

## Known Paths
- Nuclei templates: `C:\Users\bryan\.nuclei-templates`
- Go httpx: `C:\Users\bryan\go\bin\httpx.exe`
- Pipeline root: `C:\Users\bryan\.openclaw\workspace\bounty-passive-pipeline`
- Pipeline cron: `67d75617-a49c-4527-a15a-04ce5ec23f60` (DISABLED)
- Discovery cron: `3ebd4e98-e4f6-4ff8-92be-5426ac903bb4` (DISABLED)
