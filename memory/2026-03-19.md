# Memory — 2026-03-19

## Session Overview
Long productive session with Infinitara covering: gateway crash analysis, full bounty pipeline build, OSINT layer, and vehicle lookup integration.

---

## Gateway Crash (21:05 UTC)
- Gateway didn't actually crash — config was hot-reloaded at 21:05:00 (likely triggered by config save)
- Telegram channel restarted on hot-reload, caused ~1s interruption
- No memory embeddings (OpenAI key out of budget — `insufficient_quota`)
- Root cause: config touch/save at 21:05. No action needed.

---

## Bounty Passive Pipeline — FULLY BUILT
Location: `C:\Users\bryan\.openclaw\workspace\bounty-passive-pipeline`

### What's running
- **Passive monitoring** (cron every 6h): scrapes Bugcrowd, HackerOne, Intigriti, Standoff365 for program listings; snapshots; diffs for scope/reward/technique changes
- **Active scanning** (triggered when new assets found): StackDetector → picks dalfox/sqlmap/nuclei/auth/api scanners based on detected stack
- **3 cron jobs**: pipeline+scan (every 6h), discovery (daily 8am UTC), nuclei templates (Sundays 3am UTC)
- **DRY_RUN: false** — live reporting active

### Key files
- `src/index.ts` — CLI with `--once`, `--scan`, `--osint`, `--program`, `--platform`, `--discover-only`
- `src/Scheduler.ts` — continuous watch loop
- `src/Reporter.ts` — orchestrates classify → write → Telegram
- `src/scanner/ScannerOrchestrator.ts` — runs XSS/SQLi/SSRF/Auth/API/Nuclei based on stack detection
- `src/stackdetector/StackDetector.ts` — fingers stack via headers/HTML/JS/favicon
- `config.json` — DRY_RUN=false, SCANNER_ENABLED=true, all scanner tools enabled

### Active scanning tools checked (installed on system)
- dalfox (XSS) — available
- sqlmap (SQLi) — available
- nuclei (template scanning) — available
- hydra (auth brute) — NOT confirmed available
- gxss (SSRF) — available

---

## OSINT Layer — FULLY BUILT
Location: `C:\Users\bryan\.openclaw\workspace\bounty-passive-pipeline\src\osint\`

### 8 collectors built
- `PersonCollector` — Google search, LinkedIn, Twitter, haveibeenpwned, namechk
- `BusinessCollector` — Companies House, SEC EDGAR, opencorporates, WHOIS
- `VehicleCollector` — DVLA + car-checking.com + NHTSA VIN + MIB Navigate
- `PhoneCollector` — NumValidate, breach lookups
- `EmailCollector` — HIBP, Hunter.io
- `DomainCollector` — WHOIS, crt.sh (cert transparency), DNS records
- `IpCollector` — BGPView API, port banners, ip-api.com geolocation
- `UsernameCollector` — 50+ platform profile checks

### CLI usage
```bash
npm run osint -- --osint <type> <target>
# types: person, business, vehicle, phone, email, domain, ip, username, general
```

### Triggered in chat — just ask me directly and I'll run it

---

## Vehicle Lookup — WORKING
Tested on KY05YTJ (Vauxhall Corsa SXI CDTI, black, 2005)

### Sources (free, no API key)
1. **GovUK-DVLA** — make + colour (Playwright, works)
2. **car-checking.com** — full MOT history, specs, fuel economy (Playwright, works, returns comprehensive data including failures and advisories)
3. **NHTSA vPIC API** — VIN decode, no auth needed (simple HTTP GET, works)
4. **MIB Navigate** — insurance check (HTTP POST, returned 403 — blocked)

### Known blocked
- GOV.UK MOT History (check-mot.service.gov.uk) — Incapsula blocked from this server IP
- MIB Navigate insurance check — HTTP 403

### car-checking.com form details
- URL: `https://www.car-checking.com/report` (POST)
- Input: `#subForm1` (name=`i`)
- CSRF token: `_token` (hidden field on homepage)
- No bot protection detected
- Returns: make, model, year, engine, fuel, MOT history with failure details, advisories

### Plate formats supported
- UK current (2+2+3): KY05YTJ ✅
- UK prefix (1+1-3+2-3): e.g. A123ABC
- UK suffix (3+1-3+2): e.g. ABC123DE
- VIN (17 chars) → NHTSA lookup ✅
- US plates → state hint only

---

## User Preferences / Notes
- Infinitara prefers candid, concise responses — no filler
- They set up the bounty pipeline and want it fully autonomous
- Gateway runs on Windows (ThePC)
- Wants OSINT triggered on-demand via chat
- No paid APIs for vehicle data (DVLA paid API excluded)
- car-checking.com is the primary free MOT/spec source to use

---

## Git Commits Today
- `215289f` bounty: go live - DRY_RUN=false, SCANNER_ENABLED=true
- `9c91e8a` osint: full on-demand OSINT layer - 8 collectors, all types, Telegram delivery
- `08a463d` osint: vehicle - DVLA + car-checking.com (MOT history, specs, fuel economy) + VIN via NHTSA
