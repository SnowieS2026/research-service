# Daily Notes -- 2026-03-28

## Prismal Pipeline -- Major Session

### Issues Fixed Today

**Token limit truncation (root cause of missing sections)**
- `writer.ts`: `num_predict` was 4,000 -- model hit cap mid-output, cutting newsletters off mid-sentence
- Fix: raised to 8,000. Newsletter now completes fully (verified: all sections present, proper footer)
- Lesson: 4,000 tokens = ~2,500-3,000 words output. For newspaper-depth 6-8 section newsletter this is insufficient. 8,000 is the right cap.

**X character limit**
- User wants X posts at 260 chars max (not 280)
- Updated `platform-formatters.ts` `formatX()`: `maxOpener` changed from 194 to reflect 260 limit
- All platform limits now: X=260, X Thread=260, TikTok=220

**X Thread over-limit tweets**
- Tweet 3 was 264 chars -- big story truncated mid-word at 500-char boundary, inflating second chunk
- Fix: `formatXThread` now truncates big story at word boundary (~465 chars) not character boundary
- WHAT TO WATCH items pre-truncated to 220 chars to fit within 260 limit after prefix

**Substack formatter upgrades**
- Now includes all stories per section (was: `slice(0, 2)` = only 2 stories)
- Full paragraphs from parser (parser summary limit raised from 300 to 2,000 chars)
- Includes "SIGNALS FROM THE EDGE" section

### Format Locked (user confirmed)

Dashboard: `prismal-pipeline/reports/distribution/dashboard-2026-03-28.html`
- Each platform rendered as inline HTML in its own section (not copyable textarea)
- Substack: rich formatting (headers, bold, lists, horizontal rules)
- X single: tweet-style display with tag chips
- X Thread: per-tweet cards with thread numbers
- BeeHiiv: live iframe preview
- Copy buttons for each platform
- Tags shown as plain text chips (no `#` prefix in display sections)

Pipeline files committed: `src/platform-formatters.ts`, `src/dashboard.ts`, `src/writer.ts` + dist/

### Issue 1 Status
- Generated: `reports/daily/2026-03-28.md` -- 17,992 bytes, all sections complete
- `.issue-lock` not yet created -- user needs to confirm Issue 1 is locked before advancing
- Issue 1 platform outputs verified all within limits:
  - Substack: 5,528 chars
  - X single: 259/260
  - X Thread: 10 tweets all <= 260
  - TikTok: 182/220
  - BeeHiiv HTML: 12,754 chars

### Dashboard Generation Note
`--dashboard` flag is NOT wired into `index.ts`. Dashboard is generated by running `generateDashboard()` from `dist/dashboard.js` directly:
```
node -e "const {generateDashboard}=require('./dist/dashboard.js');generateDashboard('reports/daily/2026-03-28.md','reports/distribution',1)"
```
Or add `--dashboard` support to `src/index.ts` if desired.

### No Emdashes
Pipeline source code uses `--` as comment markers but emdash character (---) is banned from all output. `fix-encoding.py` strips Chinese character artifacts from model output. `beehiiv.ts` `markdownToHtml()` also strips emdashes from converted HTML.

## Model Assignments (updated)
- Primary: `minimax-m2.7:cloud` (general/reasoning)
- Coding: `qwen3-coder-next:cloud`
- Research: `glm-5:cloud` (also used for daily geo reports)
- Fast: `nemotron-3-nano:cloud`
- Flipping: `qwen3.5:cloud`
- Ollama Connect: all models use `:cloud` suffix, no local models registered in config

## OpenClaw Terminal Stall
Still present -- blocking exec calls in main session freeze replies. Workaround: use isolated subagent sessions for background work.

## Session 2026-03-28 Later -- Fixes Round 2

### Signals Not Appearing in Substack/BeeHiiv (Fixed)
Root cause: `parseSignals()` in `dashboard.ts` used `extract()` helper which stripped `###` heading markers before parsing. Signals section uses `###` headings (from GLM-5 writer) but parser expected `**bold**` format.
Fix: `parseSignals()` now iterates raw markdown lines directly, detecting `### ` title lines and blank-line-separated content paragraphs.

Second bug: SEO `<!--` metadata block was being absorbed into the last signal's content (signal 3 ended with keywords/description instead of proper ending).
Fix: parser now stops at `<!--` opening comment line (in addition to standalone `---` divider).

Third bug: BeeHiiv formatter had `s.content.slice(0, 200)` truncating each signal to 200 chars with `...`.
Fix: removed the slice -- full content now renders.

### Substack Header Format (Fixed)
Old: `# {headline}` / `*Saturday, 28 March 2026 -- Issue 1*`
New:
```
⬡ Prismal ✍️ | Newsletter | Issue 1
Saturday, 28 March 2026 · Issue 1 · Tech | Finance | Geopolitics
```
Format is computed from `dateIso` + `issueNumber` in `formatSubstack()` -- consistent across all future issues.

### Big Story X Thread Over-Limit (Fixed)
Tweet 2 was 313 chars -- word-splitting approach failed on long unpunctuated paragraphs.
Fix: `formatXThread()` big story chunker now uses word-boundary splitting at 230 chars per chunk (always within 260 even with prefix).

### All Platform Char Counts (Final)
| Platform | Chars | Status |
|----------|-------|--------|
| Substack | 13,242 | OK |
| BeeHiiv | 18,269 | OK |
| X single | 248/260 | OK |
| X Thread | 15 tweets all <=260 | OK |
| TikTok | 182/220 | OK |

### Commits Today
- `9378d89f`: Fix: token limit 8k, X limit 260, X Thread word-boundary splits, Substack full stories
- `28c61fc1`: Fix parseSignals to handle ### headings; fix big story word-boundary split; Signals now appear in Substack and BeeHiiv
- `d1ae7c4b`: Fix: Substack header format, Signals SEO bleed, BeeHiiv full Signals, date formatting

### Pipeline Config (locked for cron)
- Model: `glm-5:cloud` (deep research, 16k tokens) in `writer.ts`
- Token limit: `num_predict: 16000` in `writer.ts`
- Queries: 38 queries across all beats and regions in `index.ts` `DAILY_QUERIES`
- Article content slice: 1,500 chars in `platform-formatters.ts` `formatArticle()`
- Top story content slice: 3,000 chars in `platform-formatters.ts` `formatTopStory()`
- Writer agent: `agents/writer-agent.md` (Signals: 3-5 items, 4-6 sentences each, investigative depth)
- Dashboard generation: `generateDashboard()` called directly from `dist/dashboard.js` after pipeline run


## Contacts

**Infinitara** (851533398) -- primary user, Telegram @Infinitara
**Shell** (5958934302) -- authorized sender, Telegram @shell_biatch
