# Memory — 2026-03-26

## Session Overview

Heavy session: vehicle OSINT rebuild, vector memory overhaul, profile compilation.

---

## 🚨 Critical Bug Found — AJ05RCF Advisory Miss

**What:** Old vehicle-osint.py pipeline was missing 26 of 27 MOT advisories.

**Root cause:** `estimateAdvisories()` used a hardcoded keyword dictionary (~50 entries). If a MOT term wasn't in the dictionary, it silently returned no match. The Mondeo's brake pipes, suspension bushes, multiple coil springs — none were in the keyword list.

**What I told Infinitara last night (lost to compaction):** "Only needs new tyres at £50-80, clean car, scope for £500 profit" — WRONG.

**What the full MOT text shows:** 27 advisories across 19 tests, including:
- Brake pipe corroded (safety critical)
- Subframe bush worn (x3)
- Coil spring corroded (x4)
- Brake hose deteriorated (x5)

**Advisory cost reality:** £1,220–£5,440 estimated repair costs.

**This could have bankrupted Infinitara** before even starting bounty work. He had arranged a viewing and was planning to buy based on my bad advice.

**Lesson:** Always parse the actual MOT text directly — never rely on a keyword dictionary against raw text when the MOT has structured data. The new parser splits each MOT test block individually and extracts every `Advice` line.

---

## Vehicle OSINT — Complete Rewrite

**Files changed:**
- `vehicle-osint.js` — fully rewritten (~81KB)
- `reports/osint/2026-03-25/vehicle-AJ05RCF.md` — new comprehensive report

**What was fixed:**
1. `parseSpecSection` — rebuilt with independent per-field regexes (was bleeding: "CO2 emission 192 g/km CO2 label J" → co2_gkm got "192 g/km CO2 label J")
2. MOT block parser — splits each `MOT #N` block individually, extracts all `Advice` lines per test (was concatenating everything into 1 advisory)
3. Mileage timeline — now uses embedded `dataMileage` JS arrays from page, reversed to newest-first
4. Advisory cost database — 40+ entries across 4 severity tiers
5. Valuation engine — log depreciation by age, mileage adjustment, condition scoring, as-is value

**Test result (AJ05RCF — 2005 Mondeo LX):**
- 23 fields extracted (was 7)
- 27 advisories found (was 1)
- Retail: £700-750 | As-is: £250-350 | Repair cost: £1,220-5,440
- Recommendation: Do not buy at £650 — negotiate to £150-200 or walk

---

## Vector Memory — Complete Overhaul

**Problem:** Old `vector-store.ts` used chromadb Node SDK which called Chroma's deprecated v1 API (410 Gone). The library silently fell back to a dummy hash-based embedder — it appeared to work but returned garbage embeddings.

**What was built:**

### 1. `src/vector-store.ts` — rebuilt
- Direct Chroma v2 REST API client (no SDK)
- Collection UUID lookup at startup (SDK created collections with different UUIDs than direct API)
- Proper snake_case JSON for v2 API
- No stderr noise — SDK warnings eliminated

### 2. `auto-ingest.ts` — new
- Incremental sync, cursor-based (tracks mtime per file)
- Only re-ingests changed files
- Cursor stored at `logs/vectorstore/.sync-cursor.json`
- Scopes: memory/*.md, persona files, pipeline research, vehicle reports (last 7 days)

### 3. `session-startup.ts` — new
- Runs at session start (wired into AGENTS.md step 5)
- 5 topic queries: recent sessions, vehicle OSINT, bounty pipeline, user prefs, OpenClaw config
- Prints hits to stdout for session context

**Commit:** `817ebbf6` — "vector-memory: rebuild vector-store.ts with Chroma v2 REST API, add auto-ingest.ts + session-startup.ts, wire startup query into AGENTS.md"

**Chroma v2 API path:** `/api/v2/tenants/default_tenant/databases/default_database/collections/{uuid}/*`
**Collections:** `agent_memory` (29 docs indexed) | `pipeline_findings` (0 — needs ingest-pipeline.ts run)

---

## Edit Tool — `~` Path Failures

**Problem:** Using `~` in file paths caused silent failures in the edit tool on Windows. The tool received `~\.openclaw\workspace\...` but couldn't resolve it.

**Root cause:** `~` is PowerShell's `$HOME` alias, but edit tool runs outside PowerShell process context. `HOME` env var was set to `C:\Users\bryan` but the tool's path resolution didn't use it.

**Fix applied:**
- Added to Red Lines in AGENTS.md: "Never use `~` in file paths"
- Updated USER.md known issues: "Never use `~` — use full absolute paths"
- Committed: `44dde3bc`

**Going forward:** Always use `C:\Users\bryan\.openclaw\workspace\...` — never `~`.

---

## PROFILE.md — Comprehensive Agent Profile Created

**Location:** `C:\Users\bryan\.openclaw\workspace\PROFILE.md`

**Contents:**
- Full capabilities inventory (memory, web, code, OSINT, automation, analysis)
- Current revenue streams (bug bounty PAUSED, vehicle OSINT ready, sector research ready)
- Tool inventory (all scanners in PATH, Chrome/Playwright, SearxNG, Chroma, Ollama)
- Revenue strategy (save via car deals, earn via bounty, research for timing)
- Memory architecture diagram
- How to use me effectively (per revenue stream)
- Next growth priorities

**Commit:** `f0a6b995` — indexed to vector store (1 new doc, 29 total)

---

## Git Commits This Session

| Commit | Message |
|--------|---------|
| `fb26af50` | vehicle-osint: comprehensive rewrite - spec/MOT/mileage parsers, advisory DB, valuation engine, full report |
| `817ebbf6` | vector-memory: rebuild vector-store.ts with Chroma v2 REST API, add auto-ingest.ts + session-startup.ts |
| `44dde3bc` | rules: add no-tilde path rule to Red Lines; update USER.md known issues |
| `f0a6b995` | PROFILE.md: comprehensive agent profile - capabilities, tools, revenue streams, strategy |
| `fe282146` | update MEMORY.md with PROFILE.md reference |

---

## Notes

- Infinitara has a viewing for AJ05RCF (2005 Mondeo LX) tomorrow. He was planning to buy based on bad advice from last session. He's been warned — the car has serious advisory load.
- Bounty pipeline remains PAUSED. All crons disabled 2026-03-25.
- Window to re-engage bounty: once Infinitara confirms he's done with car buying, offer to reactivate the pipeline crons.
- Vector store auto-ingest runs every ~3 heartbeats. Session startup query runs every new session.
- `pipeline_findings` collection is empty — `ingest-pipeline.ts` needs to be run to populate it with historical scan data.
